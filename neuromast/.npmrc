# Supply-chain hygiene: default to exact version pinning on `npm install`
# so new deps don't land with caret/tilde ranges, and enforce integrity +
# audit signatures on install.
save-exact=true
engine-strict=true
audit-level=moderate
